# 코드 리뷰 및 사용성 개선 — 2026-09-05

기존 React·Zustand·Tailwind 구조와 계산 기능을 유지하며 데이터 손실 가능성과 모바일 작업 흐름을 우선 개선했다. 운영 사이트 배포는 실행하지 않았다.

## 주요 발견과 수정

| 중요도 | 발견한 문제 | 수정 |
| --- | --- | --- |
| 높음 | 앱 시작 시 `data-version`이 다르면 `localStorage.clear()`를 실행해 개인 레시피와 설정까지 삭제 | `src/bootstrap.ts`에서 스토어 복원 전에 최초 데이터만 준비. 기존 저장본과 의도적으로 비운 목록 보존 |
| 높음 | 목록 진입 시 모든 레시피에 출처가 없으면 샘플로 덮어씀 | `RecipeListPage.tsx`의 자동 교체 제거 |
| 높음 | 단계별 레시피의 평면 재료만 저장하여 다시 열면 `phases`의 이전 중량이 복원됨 | `syncRecipePhases`로 두 표현 동기화. 단계 ID와 추가 메타데이터 유지 |
| 높음 | 편집기가 `instructions`만 사용해 `steps`의 공정을 표시하지 못함. 제법 객체도 선택기에 직접 전달 | 편집 입출력 정규화. 설명·제법·공정 시간·온도 보존, 중량 숫자 변환 |
| 높음 | 브라우저 저장 한도 초과를 콘솔에만 기록하면서 성공 메시지 표시 | 저장 실패 상태와 지속적인 화면 안내 추가. 실패 시 저장 성공 알림 방지 |
| 중간 | 상세 화면에서 삭제를 누르면 즉시 제거 | 확인·취소 제공. 확인 창에 포커스 이동, Escape 닫기, 키보드 포커스 순환과 복귀 |
| 중간 | 재료 입력기의 Tab/Shift+Tab을 가로채 역방향 이동을 방해 | 기본 Tab 이동 복원. Enter로 다음 행 이동은 유지하고 한글 조합 중 Enter 보호 |
| 중간 | 새 편집 화면에서 저장할 대상이 없으면 저장 콜백이 아무 작업도 하지 않음 | 새 레시피 저장 분기 추가 |

핵심 관련 파일: `src/utils/storage/bootstrap.ts`, `src/utils/recipeEditing.ts`, `src/stores/useRecipeStore.ts`, `src/components/dashboard/AdvancedDashboard.tsx`.

앱 마운트 이후에도 초기 안내문이 잠깐 화면 전체를 차지하는 현상을 배포 빌드 캡처에서 확인했다. 100ms 주기 감시 대신 React 마운트 시점에 즉시 숨기도록 수정했다.

## 화면 변경

- 모바일 변환기를 **재료 입력 → 변환 설정 → 계량 결과 → 공정·메모**의 네 작업 화면으로 구분했다. 이동해도 같은 컴포넌트의 편집·계산 상태를 유지한다.
- 재료명과 그램 입력을 넓게 배치하고 분류·단계는 보조 행에 배치했다. 숫자 입력에 소수점 키보드를 지정하고 작은 글씨 입력으로 인한 휴대폰 확대를 줄였다.
- 저장을 상단에서 찾을 수 있도록 배치하고, 복사·JSON·인쇄·타이머·초기화는 도구 메뉴로 모았다. 기존 간단/전문가 보기 선택은 유지한다. 모바일에서는 간단 보기에서도 팬·오븐·제법 설정에 접근할 수 있다.
- PC 원본/결과 비교와 패널 크기 조절은 유지했다. 비교 표는 충분한 너비에서 나란히 표시하고, 작은 노트북 폭에서는 세로 배치한다. 행 높이와 패널 여백을 늘렸다.
- 홈 통계를 간결하게 정리하고 바로가기·최근 레시피의 간격을 넓혔다. 목록의 검색·필터와 콘텐츠 영역을 구분했다.
- 모바일 설정은 9개 항목을 일반·계산·시스템 그룹으로 선택한다. 가로로 밀어야만 보이던 설정에 바로 접근할 수 있다.
- 하단 메뉴가 편집/상세에서도 레시피 위치를 표시한다. 기기 하단 안전 영역, 본문 건너뛰기, 키보드 포커스, 동작 줄이기 설정을 반영했다.
- 기본 편집기의 저장·취소는 하단에 고정하고 재료 표를 모바일 입력 배열로 바꿨다.
- 저장/작업 알림은 모바일에서 하단 메뉴 위로 배치한다. 버튼 배경 대비를 높였다.
- 모바일 공정 화면에서 인쇄해도 원본과 계량 결과를 함께 출력한다.

## 검증

- 단위 테스트 **172개 통과**. 기존 계산 테스트와 초기화·공정/단계 보존·저장 실패 회귀 테스트 포함.
- Chrome 및 Playwright의 iPhone 12 WebKit 환경에서 **26개 브라우저 테스트 통과**.
- 입력 → 배수 변경 → 결과 확인 → 공정 → 저장 → 재열기, 9개 설정 분류, 삭제 확인/취소, 편집기 키보드 이동, 인쇄 영역을 검증했다.
- 360·390·768·1440px에서 홈·목록·변환기·설정·DDT 화면과 모바일 작업 단계의 가로 넘침/실행 오류를 검사했다.
- `npm.cmd run build` 통과: TypeScript 검사, 배포 파일과 PWA 서비스워커 생성 완료.
- Windows WebKit의 한글 경로 녹화 오류를 재현하여 해당 환경만 영상 녹화를 끄도록 테스트 설정을 보정했다. 화면·기능 검증은 그대로 실행한다.

재실행:

```powershell
npm.cmd run test:run
npx.cmd playwright test tests/e2e/responsive-workflow.spec.ts tests/e2e/basic-functionality.spec.ts --project=chromium --project="Mobile Safari" --workers=2 --reporter=line
npm.cmd run build
node scripts/review-responsive.mjs
```

화면 점검 스크립트는 기본적으로 `http://127.0.0.1:5173`을 사용한다. `REVIEW_URL` 환경 변수로 배포 빌드 미리보기 주소를 지정할 수 있다. 캡처와 측정치는 `.review-shots/`에 저장된다. 독립 브라우저 컨텍스트를 사용하므로 실제 사용자 저장본에는 접근하지 않는다.

## 검증 범위와 남은 구조적 사항

- 실물 휴대폰의 소프트 키보드, PWA 설치 및 백그라운드 타이머는 이번 자동 테스트 범위에 포함되지 않는다.
- 기존의 모든 JavaScript E2E 파일을 실행한 것은 아니다. 위에 명시한 기본 동작 및 신규 회귀 테스트를 검증했다.
- `AdvancedDashboard.tsx`는 여전히 큰 컴포넌트이며 계산·편집·저장의 결합도가 높다. 단계 데이터 동기화와 편집 입출력은 별도 유틸리티로 분리했지만 전체 계산 구조 재작성은 하지 않았다.
- 편집 중 다른 앱 메뉴로 이동할 때의 초안 복원은 별도 개선 대상이다. 저장 후 재열기와 모바일 내부 작업 메뉴 간 상태 유지가 이번 검증 범위다.
- 빌드에는 기존 Browserslist 데이터 갱신 안내와 DDT의 정적/동적 중복 import 경고가 남는다. 이번 변경에서 의존성 업그레이드나 HMR 로딩 구조 변경은 하지 않았다.


## 배포 전 추가 보정

- 사용자 피드백을 반영하여 재료 표의 고정 높이와 내부 세로 스크롤을 제거했다. PC 상단의 중복 제목 영역을 줄이고 저장을 도구 옆에 배치했다.
- 녹차 롤케이크 9개 재료는 1440×900 화면에서 모두 표시된다. 24개 재료도 표 내부에서 숨기지 않고 전체를 펼친다. 화면보다 긴 레시피는 문서 자체가 길어질 수 있다.
- PWA의 `/manifest.json` 경로를 실제 생성 파일 `/manifest.webmanifest`로 수정했다. 캐시 활성화 시 현재 Workbox 캐시와 사용자 데이터 캐시가 지워지지 않게 정리 범위를 제한했다.
- 사용자 정의 fetch 처리와 프리캐시의 응답 충돌을 없애기 위해 Workbox 라우트 순서를 적용했다. 참고: [Workbox precaching 공식 문서](https://developer.chrome.com/docs/workbox/modules/workbox-precaching).
- 배포 빌드에서 서비스워커 제어, 프리캐시 보존, 오프라인 재진입과 레시피 열기를 검증했다. `node scripts/verify-deployment.mjs`로 재실행할 수 있다.
